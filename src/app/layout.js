
import "./globals.css";


export const metadata = {
  title: "Gerencia de Memória",
  description: "Simulador de algoritimos de gerenciamento de memória",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-br">
      <body
        
      >
        {children}
      </body>
    </html>
  );
}
